; Wizards are tried in declaration order. Listing a wizard REPLACES the
; built-in default, so the config wizard must be declared explicitly to
; keep reading pjsip.conf alongside the database.

[res_pjsip]
endpoint=config,pjsip.conf,criteria=type=endpoint
endpoint=realtime,ps_endpoints

[res_pjsip_endpoint_identifier_ip]
identify=config,pjsip.conf,criteria=type=identify
identify=realtime,ps_endpoint_id_ips

[res_pjsip_authenticator_digest]
auth=config,pjsip.conf,criteria=type=auth
auth=realtime,ps_auths

[res_pjsip_registrar]
aor=config,pjsip.conf,criteria=type=aor
aor=realtime,ps_aors
contact=realtime,ps_contacts