spring:
  application:
    name: pbx-api

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:asterisk}?stringtype=unspecified
    username: asterisk
    password: dev_only_not_secret

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

logging:
  level:
    com.voipplatform: DEBUG